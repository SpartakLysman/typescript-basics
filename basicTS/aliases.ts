type ID = number | string;
let customerId: ID;

customerId = 99;
customerId = 'HRB*484HD11'

type Rectangle = { length: number, name: string };
let myRoom: Rectangle;
myRoom = { length: 88, name: 'room name' };
console.log(myRoom);
